/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java Library project to get you started.
 * For more details take a look at the Java Libraries chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.10.3/userguide/java_library_plugin.html
 */

//这里只能使用https://plugins.gradle.org里提供的插件
plugins {
    // Apply the java-library plugin to add support for Java Library
    id 'java-library'
    id 'org.springframework.boot' version '2.1.2.RELEASE'
	id 'java'
}

//老的调用插件的办法，使用方式就更灵活
apply plugin: 'io.spring.dependency-management'
apply plugin: 'maven'		//使用maven插件

jar {
	manifest {
		attributes 'Main-Class': 'com.youming.spring.boot.Application'
	}
}

//安装脚本
install {
    repositories.mavenInstaller {
        pom.version = "1.0"
        pom.artifactId = "sample-spring-boot-gradle"
        pom.groupId = "com.youming"
    }
}


configurations {
    // exclude Tomcat
    compile.exclude module: 'spring-boot-starter-tomcat'
}


dependencies {
    // This dependency is exported to consumers, that is to say found on their compile classpath.
    api 'org.apache.commons:commons-math3:3.6.1'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
    
    compile 'org.springframework.boot:spring-boot-starter-web'
    compile 'org.springframework.boot:spring-boot-starter-undertow'
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

// In this section you declare where to find the dependencies of your project
repositories {
    // Use jcenter for resolving your dependencies.
    // You can declare any Maven/Ivy/file repository here.
	mavenLocal()
    mavenCentral()
    maven { url 'http://maven.aliyun.com/nexus/content/groups/public/' }
    maven { url 'http://maven.aliyun.com/nexus/content/repositories/jcenter'}
}
